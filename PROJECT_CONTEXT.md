# Office Craft

This project is already developed.

DO NOT redesign existing pages.

DO NOT rebuild working UI.

DO NOT replace components unless specifically instructed.

DO NOT change routing structure.

Always modify the existing implementation.

Preserve spacing, colors, typography and layout.

Reuse existing components whenever possible.

Prefer editing files instead of replacing them.

If a feature already exists, extend it.

Never generate a new dashboard if one already exists.

Never rename folders unless requested.

Keep code style consistent with the repository.

When uncertain, ask before making architectural changes.
